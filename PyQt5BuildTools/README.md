# PyQt5BuildTools
这是一个PyQt5构建工具，功能简单，可以将ui文件转换为python代码（需要安装PyQt5设计器）。

还可以把生成后的python代码打包成Windows可执行文件.exe（需要pyinstaller）

先把.ui文件准备好，再生成python代码，之后才能打包为exe文件。

生成python代码会先生成一个.py文件，然后是main.pyw，是一个能运行显示界面的程序
